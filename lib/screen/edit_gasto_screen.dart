import 'package:flutter/material.dart';

class EditGastoScreen extends StatelessWidget {
  const EditGastoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Editar Gasto')),
      body: const Center(child: Text('Pantalla de edición')),
    );
  }
}
